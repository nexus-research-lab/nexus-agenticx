"""Sample module for outline tests."""

class SampleClass:
    """A demo class."""

    def greet(self, name: str) -> str:
        """Say hello."""
        return f"hello {name}"


def standalone_fn(x: int) -> int:
    """Double the input."""
    return x * 2
