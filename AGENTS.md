# AGENTS.md

> **Brand note (2026-05-24):** The Desktop app was renamed from **Machi** to **Near**. Existing references to "Machi" in plan files, conclusions, and historical docs are intentionally preserved.

## Learned User Preferences
- 默认使用中文回复；技术术语可按需保留英文。
- 进行 git commit 时，提交信息必须包含 `Made-with: Damon Li`，并偏好按功能点分组、附结构化需求块（如 FR/NFR/AC）。每个 commit 还须标注**过程元数据** trailer：`Plan-Model`（做 plan 用的模型）与 `Impl-Model`（做实施用的模型）——这不是 AI 署名（author 始终是 Damon Li），仅记录生产工具链；取值由用户提供，未提供时**主动询问**、禁止编造，纯实施类小改动可只写 `Impl-Model`。白名单 trailer 为 `Plan-Id` / `Plan-File` / `Plan-Model` / `Impl-Model` / `Made-with: Damon Li`，顺序自上而下，其它一律禁止。
- Plan 文档必须落盘到 `.cursor/plans/` 并随代码一起提交，不能遗漏；plan 顶部（标题之后）应记录规划模型，如 `Planned-with: <模型>`，让 plan 自描述。
- Plan 还须给出**推荐实施模型（Suggested-Impl-Model）**：以**高性价比**为首要目标，按子任务性质（后端接线/简单 API/前端视觉品味/跨栈高风险收口等）从当时可用模型清单中匹配「够用且最省」的模型——不为简单样板任务上顶配、也不用弱模型碰高回归/高风险/需审美的活；主规划宜提供一张「子规划 → 推荐模型 + 理由」表，各子规划顶部补一行 `Suggested-Impl-Model: <模型>`。该推荐仅为建议，最终 `Impl-Model` trailer 仍以实际使用为准、由用户确认。判据示例（随模型清单更新迭代，不硬绑具体型号）：Composer/Fast 档与代码专精便宜档（如 Kimi Code/GLM）适合简单 CRUD 样板与骨架；代码专精中档（如 Codex 系列）适合后端实施；强推理档（如 GPT-5.x）适合跨栈高风险收口与序列/一致性敏感改动；顶配（如 Opus 系列）留给复杂架构规划与需要视觉审美/品味的前端重塑。
- **Plan 与 git commit 信息严禁出现客户信息**：不得在 plan 标题/正文、Plan-Id slug、commit subject/body 中写入客户名称、客户代号、客户目录路径或可识别标识；一律用「Enterprise 交付」「客户项目」「外部招标文档本地目录」等中性表述；Plan 文件名也不得嵌入客户 slug。
- Desktop 端视觉重塑：App 命名为「Machi」，应用图标偏好《全职猎人》玛奇神韵的极客化解构——纯黑白高对比度矢量线稿（类 NousResearch 风格），仅保留至颈部的大头贴（无肩/无头巾），以高马尾和冷酷洞悉的眼神凸显“绝对理性”的高级开发者工具气质，拒绝低端二次元感或渐变色。
- 配置面板遵循关注分离：MCP 独立 tab 不混入 Provider；用户可改配置项必须提供 Desktop 设置面板 GUI；模型切换需持久化；新建或编辑分身时须能设置**默认模型**并真正落盘生效，点击分身或重启应用后均须按「分身 → 会话」维度回显最后选择的模型，不得退化为「未选模型」或被全局默认覆盖。MCP 市场 UX：默认（未搜索）视图仅展示官方/认证/托管精选，用户主动搜索必须返回**全量结果**，不得以「官方认证+托管+可安装」等条件继续过滤；已安装/已添加 MCP 在列表中须明确显示「已添加」（绿色状态点），不得仍保留「添加」按钮造成误点；安装/添加需有即时进度或状态反馈，失败/报错 toast 应靠近触发卡片就近展示，避免仅顶栏提示导致未上滚时看不到；列表内解释性长备注（如"点击工具名可启用/禁用 (灰色=已禁用…)"等）宜删除，用户会自行感知；各模型供应商列表侧栏状态点与详情区 ON/OFF 须一致且仅保留红/绿两态（禁用或未实际可调用为红，已配置且启用为绿）——「已配置」指 API 密钥或**自定义** API 地址至少一项非空（留空仅用官方默认 Base 不算已配置）；无密钥的本机类供应商（如 Ollama）须填写可访问的 API 地址才算已配置；清空密钥与自定义地址时应自动视为关闭；权限确认文案需对齐 Cursor，且用户选择 `Run Everything` 后必须严格生效，禁止重复弹窗询问。主要操作按钮（如保存）应使用主题层 `--ui-btn-primary-*` 变量并按 dark/dim/light 区分，避免长期硬编码 cyan。配置面板 UX 持续以 Cherry Studio 为参照：秘钥记录与校验、模型列表管理与健康检查、@切换模型回答等交互。与防休眠、非高风险自动安装等相关的**自动化/系统行为**类开关宜放在独立 **Automation** Tab（或同级分区），不与 Skills 等能力配置混放；定时任务删除等破坏性确认宜用应用内主题化弹窗（或 Electron `dialog` 配应用图标），避免原生 `window.confirm` 的系统默认图标与样式不可控。已列入自动连接名单的 MCP（`mcp.auto_connect`）应在应用启动并完成主会话恢复后对当前会话自动补连并刷新开关状态，避免每次进设置手动点一遍。分身配置弹层（如 `AvatarSettingsPanel`）的遮罩、主容器背景与输入框/卡片背景须与全局 `SettingsPanel` 语义对齐（勿用半透叠色导致表单区异常发暗或与主题脱节）。分身设置面板应以右上角 ✕ 为主关闭入口（暗色主题下勿仅依赖低对比度侧向「回退」箭头）；与 ✕ 语义重复的回退按钮宜移除。「灵魂/人设」类字段宜排在基础信息区块之下，与基础设置同屏编排。
- 模型服务交互偏好：点击「从 API 获取模型」时不得自动把所有模型加入可见列表，需先弹出可搜索的选择面板，并按行提供 `+/-` 控制「可见/不可见」；聊天界面仅展示已标记为可见的模型。该面板视觉应与设置页一致，避免过宽或过透明。
- 多分身/群聊 UX 深度对齐微信体验：支持 @提及指定分身直接回复、未 @ 时由 Meta-Agent（产品名 Machi、显示名以 Meta 窗格为准）充当项目经理统筹/兜底；未 @ 但文本明显指向某成员职责时，路由应优先由该成员实际执行与回复，而非仅靠 Meta 口头代答。用户点名某分身时，该分身应以人类用户为主答对象，避免把主对话改成 @ 组长客套；成员在回复里 @ 其他分身时，路由应尽力让被 @ 方接续发言。群聊须**默认智能路由**：不强制用户显式选择「团队策略 / 编排模式」（如 SequentialOrchestrator 等枚举），编辑/新建群聊弹窗也不应保留这类硬选项；评测/路由任务无需用户加 `/team` 前缀，由路由器按 @ 命中与 Meta 兜底自动决策。群聊与多窗格回答须支持「中途打断 + 上下文延续追问」：用户在生成中可立即停止当前回答；紧随的新追问应基于「上一轮 query + 已生成的部分回复 + 新 query」组成的延续上下文继续作答，而非简单排队等当前回答结束。用户需要能感知分身在执行（工具调用、阻塞确认、进度类信号），避免长时间只有「正在输入」却看不到在做什么。新建群聊必须默认隐式包含 Meta-Agent；消息多选需左侧打勾，多选复制须结构化（角色+时间+内容），合并转发卡片点开需见原始历史。Session 创建参考 Cherry Studio；默认布局需视觉均衡，窄屏头像自适应。设置里「群内显示名称」参与群聊上下文标注（`user_display_name`）。与 Machi 单聊时顶部身份与头像不得误标为「分身」或丢失 Machi 头像；飞书桌面绑定在 `avatar_id` 为空时默认展示名须为 Machi（不可默认「分身」），`avatarId` 为空的窗格视为元智能体并走 `metaAvatarUrl`；助手消息若 `avatarName` 为空或为「分身」，应回落父级 Machi 展示名以免顶栏与气泡不一致。
- UI 交互必须即时响应（乐观 UI）：打开窗格、删除分身/群聊、切换会话都要先响应再异步回填；删除群聊后应回退到元 Agent 界面而非停留在已删除群聊；转发卡片等消息体必须自适应大小避免内容遮挡；工作区面板：自上而下为成员区 → 工作目录列表；Spawn Agent 列表迁至工作区 Tab 右侧独立列，spawn 启动后宜自动展开该列（可手动关闭）；工作目录项右键宜用自定义上下文菜单（对齐微信式展示，避免原生浏览器菜单），含删除与「在此目录打开终端」等；终端嵌入工作区面板底部（承接原底部 Spawns 区域），支持多终端 tab 与同级目录新开终端；（成员不单独 tab）；消息操作按钮（复制/引用/收藏/转发/重试/多选）应常驻显示而非仅 hover 出现，且用户消息行固定提供重试（非仅失败时才出现）；收藏结果反馈（如「已收藏过」）须贴近该条消息下方操作按钮行展示，避免仅在侧栏或会话列表远端角落弱提示；引用必须优先使用用户选区文本（不要整段带上 `<think>`）；多选操作必须包含删除且能持久化生效；流式回答禁止出现整段重复拼接；流式输出期间不得强制锁定滚动到底部，用户需能随时向上滚动查看已输出内容；附件上传后须在输入框**上方**展示预览（对齐豆包/Cherry Studio），图片显示可点击放大的缩略图；重试或复制消息时必须保留图片附件，不得退化成纯文本；工作区 `@` 选文件宜插入 `@file[显示名](绝对路径)` 占位符，附件元数据保留 `sourcePath`，重试与会话 `context_files` 构造优先复用绝对路径；工作区目录绑定应按分身/Meta 隔离持久化：同一分身（或 Meta）下可跨其历史会话继承，跨不同分身不得自动共享或串台。输入框宜支持类微信的展开多行模式：展开时 Enter 换行，收起时 Enter 直接发送。删除所有会话后须自动新建默认 session 而非卡在「正在初始化」。分身侧栏与 Machi/群聊入口等列表选中态宜用背景层级区分，避免显眼粗白边框抢视觉。多窗格并行聊天时，各窗格的模型选择须按窗格/会话独立持久化，一侧切换模型不得误改其他窗格的模型展示或选中态；`ChatPane` 顶部模型 pill、`/api/chat` 请求与附件视觉判断等须与底部选择器一致，优先用当前窗格 `pane.modelProvider`/`pane.modelName` 派生实际请求模型，避免误用全局 `activeProvider`/`activeModel` 导致多窗格串台。历史会话面板宜支持按关键词跨会话检索（须命中消息正文，而非仅会话标题）；从结果跳转到某会话时，对话区应高亮关键词并滚动至首个命中；若命中落在默认折叠的工具调用返回文本内，应自动展开对应工具块以便可见。
- Desktop 端 Focus Mode（焦点模式 / 「灵巧模式」）：顶栏主题切换按钮**左侧**提供简洁入口（类 Siri 轨道）；进入后 Electron 无边框置顶透明窗口收敛为右上角 **280×280 圆形语音胶囊**（无聊天记录/输入框）；核心为 Machi（Meta-Agent）头像、状态音量环与小挂断钮；按住 `Esc` 或点挂断恢复原窗口。**实时语音链路**可走 OpenAI Realtime（WebRTC，SDP 经本机 `agx serve` 代理密钥）或豆包/火山端到端 WS（经由 Studio `/ws/voice/doubao` 补齐浏览器无法携带的上游 Header）；配置在「设置 → 语音」，`~/.agenticx/config.yaml` 新增 `voice:` 节 persisted。语音轮次的文本摘要写入当前元智能体会话 `messages.json`，`metadata.source = voice-focus`。Focus 模式下仍由 Meta-Agent 统一应答，不路由到分身。
- 子智能体须全链路透明（运行状态/摘要/产出/`awaiting_confirm` 与倒计时）；完成或失败后 Meta-Agent 要主动汇报；代码生成须有类似 Cursor 的流式呈现，等待期间不得只显示 ⏳⏳ 静态符号，应使用类豆包的动态三点动画。委派须为「真委派」：任务在分身真实 session 执行、历史与产出可追溯，禁止影子 spawn 顶替身份，启动后自动打开目标分身窗格；子智能体详情面板须有一键复制按钮。Desktop 端应支持「断点续开」：合盖/重开后优先恢复上次会话与多窗格状态，避免用户重新找上下文；关闭聊天窗格后再打开同一 Machi/分身窗格时，应回到该窗格最近使用的 session，而非默认新建会话。
- LLM `<think>` 推理内容必须解析为独立 ReasoningBlock：streaming 阶段 spinner + "Thinking" 标签 + 流式推理文本在同一容器内；完成后折叠为 "Thought"；禁用独立边框/白色背景/青蓝色底色——融入消息气泡（参照 Cursor 思考链 UI）。
- 用户对重复噪音提示敏感：须收敛为单次高信号、降低焦虑感；已知非视觉聊天模型下用户尝试附图时，用 Cherry 式文案「模型不支持该文件类型」等明确提示，展示在**消息列表与输入区之间的主视区**内水平居中（紧邻状态 chips、贴近输入区上方），采用黄色感叹号警示样式，避免缩在窗口右下角或边角弱提示；内部系统消息（如工具调用频率限制日志）对用户不可见。UI 文案区分语义：操作仅取消关联/隐藏时用「移除」，真正删除文件/数据时才用「删除」，避免引起用户不必要的担心。
- 出现线上异常时优先做根因排查和证据链，不接受拍脑袋修复；实现新需求时严禁乱改已有逻辑正确的代码（用户将此称为「严重倒退现象」）；已有 workspace rule `no-scope-creep.mdc` 强制约束，每次改动必须能追溯到具体需求。涉及 Enterprise 前台用户端（如 `http://localhost:3000/workspace`、`enterprise/` 下 portal/workspace）的需求，范围必须严格限定在前台用户端代码，不能误改 `desktop/` 或后台 admin-console；目标端不清时先确认。git commit 必须精准：只 `git add` 本次任务直接改动的文件，绝不把无关已修改文件一起提交。助手若声称已完成代码修改，应给出可核验信息（具体改动文件或关键 diff 要点），避免用户误判「仓库未变」。
- Desktop toolbar 按钮图标化规范：工作区→文件夹图标（tab 仅显示图标，hover tooltip 显示名称）、Spawns→机器人图标、历史→聊天气泡图标（不用时钟）；toolbar 按钮图标须与对应面板 header 图标一致；历史面板 active 状态须用 `bg-surface-card-strong text-text-strong`，与工作区/Spawns 按钮激活样式对齐；群聊模式下 toolbar 须加成员按钮，调用 `cycleSidePanel(pane.id, "members")`。侧边面板（工作区/Spawns/历史/群成员）收起用横线图标；顶栏关闭窗格（销毁）保留 ✕，与「收起面板」语义区分。Topbar 不展示当前分身名（`avatarName`），只保留展开/收起侧栏按钮。输入框「新对话」采用 Sparkles（全新对话）+ GitBranch（继承上下文）双图标按钮，tooltip 用自定义 `HoverTip`（约 280ms 延迟）代替原生 `title`，避免浏览器固有延迟；旁边下拉菜单须用 `createPortal` 挂到 `document.body`，避免被父级 `overflow` 裁掉表现为"点了没反应"。
- 技术笔记和博客偏好"图文并茂"风格，常落盘到飞书文档库（指定文档空间下）；本地长文草稿、交互式爬取备份可按日落盘仓库 `.myblog/YYYYMMDD/` 便于归档（与飞书主库并行）；回复 GitHub Issue 时要求简约、非 AI 化、英文版。
- Skills Tab（列表与筛选）：技能列表不宜仅靠固定条数「截断」造成数量误解，应提供类 Cursor 的「Show all / 查看全部」式全量展开；按来源（如 Cursor 等预设路径）筛选时，列表结果必须与所选来源语义一致；技能安全扫描或市场安装前置校验失败时，UI 须给出可读的具体原因（命中类别或规则摘要），避免仅展示泛化「高危」。查看某技能详情时，宜在 Skills Tab 内就地展开/收起（例如双击 tab 展开当前区域、关闭时复原），避免详情面板固定挤到顶栏等位置造成布局与预期不符。全局与分身均须支持**单个 skill** 粒度启停：全局列表每行开关对应 `~/.agenticx/config.yaml` 的 `skills.disabled`；分身侧在创建向导与 `AvatarSettingsPanel`「技能」区配置，默认继承全局可用技能，仅将显式关闭项写入分身 `skills_enabled`。技能列表与分身技能区的「启用」开关视觉样式宜与设置内其他开关组件一致，避免同屏多种控件语义混用。
- 设置界面（SettingsPanel）面向中文用户须全面中文化：Display→显示、Dark/Dim/Light→深色/暗灰/浅色、Permissions→权限、Ask Every Time→每次询问、Use Allowlist→白名单放行、Run Everything→全部自动执行、Computer Use→桌面操控、Agent Harness Trinity→智能体三件套。历史会话等上下文菜单（含飞书绑定、置顶相关项）亦须中文化，避免英文菜单与主界面语言不一致。表单内原生下拉（select）右侧箭头与控件右边缘宜保留适当内边距，对齐 Cherry Studio 式视觉密度。Tools（内置工具）Tab 的工具说明与分组标题须中文化；预置/内置工具分组默认宜折叠；该 Tab 应完整呈现会话可用内置工具集合，信息架构可对齐 Cherry Studio，避免仅展示文档解析子集或误导性窄标题。
- 用户档案与元智能体档案须在设置中**分区块**：**用户档案**含称呼、用户消息头像、**用户偏好与风格**（注入系统提示，localStorage）；**元智能体（Machi）**含 Machi 头像与 `SOUL.md`（全局人格），与用户偏好分区展示。`userNickname`/`userPreference` 等仍随请求 body 传后端；`meta_agent.py` 在系统提示末尾通过 `_build_user_profile_block()` 注入别称与偏好；别称在所有对话（单聊与群聊）中对 agent 均可见。产品期望用户自定义头像与 Machi/分身头像在全局头像体系统一。
- 日报/周报写作：只写架构级创新与非显而易见的技术难题才算"亮点"，不硬拔高常规工程补课或 bug 修复；`/dailyreport` 命令落盘于 `.cursor/commands/dailyreport.md`，接受口语化日期描述参数。
- Machi Desktop 主窗口宜持久化用户调整的窗口尺寸与屏幕位置，重启应用后恢复。
- 设置表单控件与耗时操作 UX 三条通用偏好：(1) 同一鉴权项（如 API Key）不要并列暴露「API Key + 环境变量名」两个输入框，保留单一输入框并配一个「测试连通性」按钮即可，对齐 Cherry Studio 的极简风格；(2) 同一设置面板顶部与底部不应重复放「保存」按钮，仅保留底部一个，顶部可保留「重置为默认」等非重复操作；(3) 耗时操作（入库/向量化/模型加载等）必须暴露真实百分比或明确阶段文字，不得长时间只有 spinner 让用户无法判断是否卡死。
- iLink/微信对话界面：助手对外展示名宜为 Machi（避免客户端默认「WeixinClawBot」等占位）；助手与用户头像宜与 Machi/用户配置一致；助手回复宜具备可读的基础 Markdown/富文本渲染。IM 侧会话列表标题忌用泛化「微信对话」「新对话」等占位；无用户实质消息的会话不宜持久化为历史条目；标题宜用首轮用户 query 的摘要或前若干字截断。外部渠道新消息写入当前绑定会话后，桌面端消息列表须即时刷新，避免依赖切换 session 后才可见。已绑定微信 IM 的 Machi 会话须在顶栏等主界面位置展示与飞书同级的「微信」可见标记，避免「已绑定但顶栏无提示」造成误判。飞书/微信的桌面绑定与解绑以历史会话右键菜单为主入口，聊天顶栏不宜再保留与右键「绑定飞书」等价的重复按钮，以免与状态标记区功能重叠、增加噪声。
- 长对话与多工具链任务：`runtime.max_tool_rounds` 等工具轮次上限宜在 Desktop 设置面板（如 Automation/Runtime）可视化调节并持久化到 `~/.agenticx/config.yaml`，避免长期依赖用户手改 YAML 才能调大配额或跑完审计类任务。
- 定时任务（`automation:*`）专属聊天记录不得复制或串入元智能体（Meta）历史；重启与会话列表仍须严格按窗格 `avatar_id` 隔离。用户触发式定时执行宜每次新开独立 session 承载；执行所用模型与自动化关键参数宜可通过设置或自然语言在 Meta 中调整，避免「仅手改 YAML、对话侧改不动 Automation」的断层。
- 调度自动化打开 Automation 聊天窗格时，用户期望看到**当次运行的新 session**，不得在新 session 创建完成前向 UI 透出旧 `sessionId`；窗格绑定的 `sessionId` 一旦变更，须先清空窗格内消息再加载新会话，避免残留上一轮历史（主进程 `desktop/electron/main.ts` 的 `AutomationScheduler` / `runAutomationTaskHttp` 与 `onSessionReady`，渲染侧 `desktop/src/App.tsx` 的 `ensureAutomationPane` 等应对齐）。
- 历史会话标题（尤其中文摘要标题）在切换去其他 session 再跳回、或重启应用后，须与持久化元数据一致，不得回退为纯数字 `session_id` 占位。
- MCP（含 Docker 启动的 stdio 服务）在握手或 `discover_tools` 长时间阻塞时，设置界面应通过超时或明确失败态提示原因，避免长期停留在「连接中：握手并发现工具…」且无反馈。
- Enterprise 前后台视觉基线统一转向 vben 风格 **indigo/violet primary + OKLCH tokens + 系统/亮/暗三态主题**（不做 dark-only）；大视觉重构偏好「一次大 PR + 三段可验收 commit（`ui-foundation` → `admin-revamp` → `portal-revamp`）」，每段须独立 `typecheck + build` 绿后再进下一段。客户登录页/品牌区不得展示虚构或不可核实的「已服务企业」类客户名单；左侧品牌区宜松弛大气（栅格比例拉开、字号/行距/留白放大），避免密集拼贴。
- Enterprise 前台 web-portal 聊天空态偏好对齐 **Kimi 式**：无消息时顶栏不展示「分享会话」等二级动作，待 `messages.length > 0` 再出现。侧栏**折叠**（如窄到约 72px）时，主操作用 `size="icon"` 的按钮勿再叠 `w-full`+`justify-start`（易致图标不居中）；收展控制宜放在**顶栏品牌行**（纯图标），主导航区用纵向堆叠。
- 面向客户的技术评分项须用「业务演示 + 通用描述」语言，严守「满足得 X 分，不满足不得分」二元判定，技术档次（1/2/3 档）标注在评分项标题中，避免暴露 RS256/Blake2b/`oklch()` 等实现细节。起草前必须逐项回溯仓库**真实完成度**，坦诚区分「可现场演示 / 需小改 / 模板空壳不可演示」，不因一时好看而承诺仓库尚未落地或只有模板的能力。
- Enterprise admin-console **策略样本测试**：`blocked` 仅在动作为 **拦截（block）** 时为 true；**警告（warn）** 仍可有 `hits` 但不算拦截；测试接口应合并当前表单预览（未保存的 action/payload）与库内规则，避免界面选「拦截」却仍按库里旧动作计算。动作与类型在中文界面宜展示为拦截/脱敏/警告、关键词/正则/PII，**脱敏（redact）** 为命中替换占位符而非等同拦截。规则迁 PG 后支持「草稿/发布 + 部门/用户级 scope」，仅**已发布**才进入网关快照；删除等破坏性操作必须二次确认，未发布规则的删除应为**软删除**（行置灰 + 显眼红色「恢复」按钮），灰色规则不可再编辑只可恢复或永久删除（再次确认后真清），避免一键清空无法回滚。聊天合规拦截 UX 须与正常模型回复**视觉上明确区分**（拦截原因贴近用户语义，如「检测到银行卡号 / 命中策略 pii-bank-card」），同会话后续追问应按当前请求重新评估命中，不得简单沿用上一轮拦截结论让无关问题继续被「拦截」展示。
- Markdown 代码块在聊天气泡 / 笔记渲染中须在 light / dim / dark 三态主题下都具备可读的代码主题（syntax highlighting + 适配背景），不能在浅色主题下退化为「像把代码复制到 txt 一样」的无高亮纯字；可按主题分别配 light / dark 两套代码主题以保证对比度。
- 群聊里每个分身的过程性广播（「已接收任务 / 开始处理任务 / 正在调用工具：X / 工具已完成：X」等）会被用户视为刷屏噪音，应聚合为该分身一条可折叠的进度 / 工具状态卡（与 `ToolCallCard` 默认折叠语义一致），仅展示最终产出与必要的打断 / 确认信号；禁止每个工具调用都独立成一条群聊气泡。
- 会话状态指示器须全链路一致：同一会话在聊天气泡（如「正在输入」）、历史会话面板 / 侧栏标签等位置的状态必须同步——不能一边显示生成中、另一边显示「已中断」；空会话或尚未发出首条用户消息的会话不应预先打上「已中断」/「失败」占位，须保持「新会话」中性态。
- 保存类操作（编辑分身、编辑群聊等）必须给出即时反馈（toast 或确认弹窗），保存失败时须**保留弹窗**并透出底层错误（如 `HTTP 400 unknown avatar_id`，参考 GitHub issue #20）以便用户与反馈渠道定位；分身 `avatar_id` 等 ID 体系在新建 / 编辑落库后必须立即被后续 `/api/avatars`、群聊编辑等接口识别，禁止出现「保存看似成功，下一次请求却 `unknown avatar_id`」类断层。
- 设置 / 编辑类弹层（含群聊编辑）不可整体过透明（毛玻璃需保留主体可读对比）；对话框宽高不应被异常横向拉伸，主操作（确认 / 取消）按钮位置须保持稳定，不应漂移到中间或与主按钮错位；底部「取消 / 关闭」宜与「保存」同排相邻，**取消紧靠保存在左侧**，避免主轴 `justify`/多余 spacer 将取消键单独甩到对话框中部；面向终端用户的"功能说明 / 策略文案"长段（如群聊设置里"智能对话默认……"长备注）宜删除，UI 保持干净。
- 面向终端用户/客户的 enterprise UI（admin-console、web-portal）不得在文案、tooltip、Help 信息中暴露仓库内部路径或运维实现细节（如 `enterprise/.runtime/admin/policy-overrides.json`、`plugins/moderation-*/manifest.yaml`、「启停状态落盘到 …」等），运维存储与文件结构说明只在内部文档保留；用户对 enterprise 各模块（身份权限/审计/聊天历史/Token 用量/策略规则中心）明确要求"绝对不能再 mock"，新建 / 编辑接口须真写 PG 否则明确报错，禁止「点击看似成功但不落库」的伪成功。
- admin-console 等管理界面切换子菜单时左侧导航高亮必须跟随当前路由，不应停留在前一项；长内容子页（如「角色权限」）应让侧栏在主轴上保持固定，不与主内容一起整体滚动，避免长表格把导航栏一并下推。
- 撰写客户技术实施方案 / 投标 / 管理类长文（如 OKR 设定方案、客户应答模板）时：必须自然成文，不留 AI 文风或「知识库原回答」残痕（如「主要短板」「（你直管的部门）」等隐含组织关系的称呼），逻辑须严谨可经推敲；客户文档涉及流程图 / 架构图 / 时序图时，须先落盘 md 描述（一图一文件，置于 `images/` 等目录），再驱动 GPT Image 等模型作图；客户提到「移动云 / 客户云生态」（如对象存储、IDaaS）等替代方案时，方案以本地 / 自建为主线、运营商生态列为可选，不替客户拍板技术选型；安全策略 / 敏感词的行业 know-how（如「投资决策」「投资立项」）由客户分阶段提供，方案文档只预留接入位与少量示例，不预设完整关键词清单。
- IAM 部门管理 UI 偏好「能力卡片 / 小方块」式展示（一个部门一个卡片），不偏好层级表格树或仅列名称的密集列表；从某部门卡片跳转到「用户管理」时须按 `dept_id` 过滤展示该部门成员，不可跳到全量用户列表（曾出现「跳过去后是全量用户页」的体验回退需修复）。
- **`agenticx/studio/server.py` 是本地后端唯一入口，其顶部 import 区块（尤其 `agenticx.avatar.*` 等与 `create_studio_app()` 初始化直接相关的导入）极其敏感、不能随意改动**：曾在 2026-07-06 一次与之完全无关的附件回显修复（commit `d8428b73`）中，编辑器对相邻 import 段做整块替换时误删了 `from agenticx.avatar.group_chat import GroupChatRegistry` 这一行，导致 `create_studio_app()` 里 `GroupChatRegistry()` 抛 `NameError`、`agx serve` 启动即崩溃，Desktop 分身/历史会话/工作区全部表现为空态（数据本身完好无损，纯属这一行误删）。今后触碰该文件必须遵守：(1) 编辑 import 区或任何多行代码块时，**只能精确增删目标行**，禁止用"整段替换"方式覆盖相邻的无关行；改动前后要对照原文件逐行确认没有误删/误改任何跟本次需求无关的既有代码，这也是 `no-scope-creep.mdc` 规则在此文件上的具体落地；(2) 只要改了 `server.py`，提交前必须本地跑一次 `agx serve --host 127.0.0.1 --port <临时端口>` 冷启动验证（或等效 smoke test），确认进程不崩溃且 `/api/session`、`/api/avatars`、`/api/sessions` 等核心 API 返回 200，把"能正常起服务"作为该文件改动的强制验收门槛，不能只看 diff 语义正确就认为完成；(3) 如果表现是「Desktop 分身/历史/工作区全空」但用户没删过数据，第一优先级应排查 `agx serve` 是否存活（`serve.port` 对应端口是否真的在监听），而不是先怀疑数据丢失。

## Learned Workspace Facts
- AgenticX 全局配置目录为 `~/.agenticx`（主配置 `~/.agenticx/config.yaml`），身份与记忆目录为 `~/.agenticx/workspace`；`AGX_MAX_TOOL_ROUNDS` 等运行参数可在该文件配置。长期记忆应写入该 workspace（如 `MEMORY.md`、`memory/*.md`）并进入 `WorkspaceMemoryStore` 索引。首次启动的 Pro/Lite 模式选择由 `onboarding_completed` 写入该配置文件控制，未完成或配置重置/新环境会再次显示引导。
- 元智能体系统提示由 `agenticx/runtime/prompts/meta_agent.py` 构建，每轮动态注入活跃子智能体快照（`_build_active_subagents_context`）与记忆召回（`_build_memory_recall_context`）；`context_files` 需通过 `_build_context_files_block` 以完整内容（`--- 路径 ---\n内容`）注入系统提示，不能只展示数量，否则模型无法感知文件内容。
- 子智能体调度由 `agenticx/runtime/team_manager.py`（`AgentTeamManager`）管理，支持并发上限、归档快照、`owner_session_id` 会话隔离、`avatar_id` 绑定与跨 session 状态查询。
- `agenticx/runtime/agent_runtime.py` 会清洗上下文并强制 tool 调用序列合法，避免 `assistant(tool_calls)` / `tool` 断链导致 provider 400；streaming 路径会归一化 `None` 工具名；`[系统通知]` 不写入 `chat_history`。子智能体侧无意义重复工具调用由 `agenticx/runtime/loop_detector.py` 检测（含 tool saturation 等与 `agent_runtime` 协同）。
- `agenticx/studio/server.py` 是纯 REST API + SSE 后端（无内置 Web UI），子智能体状态/取消/重试接口支持同会话 fallback；`chat_history` 持久化到 `messages.json` 并由 `GET /api/session/messages` 暴露；群聊与普通会话需传递 `quoted_content`；批量删除消息需同步持久化；新建会话（如 `POST /api/sessions`）宜尽快返回 `session_id`，MCP auto-connect 等重操作放后台异步执行，避免「新建对话」被连接阶段阻塞。
- Stage-1 本地知识库：实现与数据在 `agenticx/studio/kb/`，Studio 暴露 `/api/kb/*`；对话侧工具 `knowledge_search` 注册于 `agenticx/cli/agent_tools.py`；`~/.agenticx/config.yaml` 的 `knowledge_base` 节驱动行为；Desktop 设置在 `desktop/src/components/settings/knowledge/`；用户文档 `docs/guides/knowledge-base-mvp.md`，计划 `.cursor/plans/2026-04-14-machi-kb-stage1-local-mvp.plan.md`；产品规划 `docs/plans/2026-04-14-machi-knowledge-base-product-plan.md`（v2.1）明确向量库默认 Chroma，并支持 Milvus Lite / Qdrant / FAISS 作为可选后端（远端可对接 Milvus Standalone/Cluster）；更换 embedding 相关配置后通常需重建索引。文档解析 pipeline 统一走 `LiteParseAdapter`，默认可解析 `.md/.txt/.pdf/.doc/.docx/.ppt/.pptx` 以及图片（OCR）；`.xlsx/.xls` 等 Office 二进制格式依赖本机 LibreOffice（macOS `brew install --cask libreoffice`），未安装时须在 UI 给出可见的安装提示并从支持列表中剔除。知识库自动检索为三态开关（智能判断默认 / 始终检索 / 仅手动），落地于「设置 → 知识库 → 检索」区块并镜像至主聊天窗格模型选择右侧的状态切换，控制 `knowledge_search` 是否被自动注入。百炼/DashScope embedding 批量调用 `batch size` 上限为 10，provider 层必须按 ≤10 条分批调用，避免 `InvalidParameter: batch size is invalid`。长耗时的入库/向量化必须向 UI 暴露真实百分比进度，不得只停留在 spinner 文案。文档解析/向量化等失败时 UI 须展示完整错误上下文（文件名、文件类型、底层异常链与堆栈摘要），不得仅打印 `'_type'`、`KeyError` 等单 token 让用户无从定位根因；parser/ingest 层捕获异常后应透传原始 traceback 摘要至前端失败详情。
- Machi 主会话流式输出依赖与当前会话绑定的 SSE：切换离开该会话时前端常会中止对应 fetch/EventSource，后端仍可能在后台跑完，但尾部 SSE（含 `final` 或限额类提示）未必送达当前窗格，易表现为「突然无输出」或 UI 与 `messages.json` 快照不一致；需与 `max_tool_rounds` 触顶、磁盘快照与内存 UI 消息两套来源等问题区分排障。
- `agenticx/runtime/meta_tools.py` 中 `spawn_subagent` 会拦截已注册 avatar 名称并引导 `delegate_to_avatar`；`delegate_to_avatar` 通过 `_find_or_create_avatar_session` + `_run_delegation_in_avatar_session` 在分身会话后台执行，provider/model 按 avatar → session → Meta-Agent 回退；`/api/subagents/status` 扫描 `_delegation_info`，委派后台任务约每 5 秒中间持久化供前端轮询。`spawn_subagent` 的 `run_timeout_seconds` 若低于默认 600s 会覆盖默认，长任务易误超时；可用环境变量 `AGX_SUBAGENT_MIN_RUN_TIMEOUT_SECONDS` 调整运行时下限（设为 `0` 可关闭抬高短超时的保护）。
- Desktop 技术栈为 React + Zustand + Electron + Vite（Pro/Lite：`ChatPane` / `ChatView`）；`store.ts` 含 `awaiting_confirm`；推理块链路需保持 `reasoning-parser.ts`、`ReasoningBlock.tsx`、`ImBubble`/`CleanBlock`/`TerminalLine` 一致。多窗格（≥3）在 `PaneManager` 中用嵌套 flex 与 `PaneDivider`（含 `vertical`）实现行列双向拖拽调宽调高，避免纯 CSS grid 无分割条不可调。DMG 自包含打包方案：`packaging/pyinstaller/agx_serve_entry.py` + `agx_serve.spec`（`collect_all("agenticx")`，非 editable 安装）+ `packaging/build_backend.sh`（venv 隔离 + `curl --noproxy` smoke 验证）；已有 plan `.cursor/plans/2026-03-24-dmg-self-contained-packaging.plan.md`。DeerFlow 2.0 codegen 已落地三个核心机制：Guardrails（`agenticx/tools/guardrails/`，含 `GuardrailProvider` Protocol + `DenyListGuardrail`）、MCP sync（`agenticx/tools/mcp_sync_tool.py`）、Token SSE（`agenticx/runtime/usage_metadata.py`）；对应 plan `.cursor/plans/2026-03-25-deerflow-2-codegen.plan.md`。Desktop 集成终端依赖 `node-pty`（native module），每次 Electron 版本升级后必须重跑 `npx @electron/rebuild -f -w node-pty`；`checkAgxCli()` 超时须设 ≥30s（Python 冷启动实测 ~9s）；pty spawn 用 `-i` flag 并备用路径 chain（`$SHELL` → `/bin/zsh` → `/bin/bash`）。Machi Electron 主进程须 `app.requestSingleInstanceLock()` 与 `second-instance` 聚焦已有窗口，避免 macOS 多进程双窗；`BrowserWindow` 使用 `show: false` 并在 `ready-to-show` 后再显示；打包 `loadFile` 失败须内嵌错误页而非静默吞错。修改 `desktop/electron/main.ts` 或新增 IPC 后须重新编译进 `dist-electron/` 并完全退出再启动（⌘Q / 重启 `npm run dev`），仅刷新渲染进程不会加载新 IPC handler。工作区面板内嵌终端区域须用主题 token（如 `bg-surface-card`），禁止硬编码 `bg-[#0f1014]`；`@xterm/xterm` 默认 `.xterm-viewport { background: #000 }` 在 `desktop/src/index.css` 覆写为透明；`TerminalEmbed` 用面板计算背景色与 `getTerminalAnsi(theme)` 对齐 light/dim/dark。聊天窗格状态区「Context」类 chip 展示本会话由 SSE `token_usage` 累加的 token 消耗（勿与附件数量混用）；另以 `localStorage` 的 `agx-session-token-cache-v1`（按 `sessionId`）与工作区快照 `agx-workspace-state-v1` 中各窗格 `sessionTokens` 协同，支持切会话与完全退出后的恢复。若历史消息或后端未持久化可聚合的 usage，旧会话累计无法自动重算，需修复后新产生的 `token_usage` 轮次才会进入上述缓存。`ChatPane` 等持久化 pane 新增字段（如 `sessionTokens`）须兼容 localStorage 恢复的旧数据（缺省合并与可选链读取），避免渲染期访问 `undefined`。工作区面板「添加文件夹/工作区」表单宜内联展开于工具栏下方，避免 absolute 浮层遮挡文件列表；展开区背景随面板 `tintColor` 与分身色一致。`ToolCallCard` 宽度与 assistant `ImBubble` 须同基准对齐（同款 `maxWidth` 与左侧头像占位结构），避免工具块与回复气泡左右边界错位。**本地 `npm run dev` 默认 Vite 端口 5713**（`AGX_DEV_PORT` 可覆盖；`strictPort: true` 禁止静默漂移）；`vite.config.ts`、`electron/main.ts` 与 `scripts/wait-and-electron.mjs` 三处对齐；`VITE_DEV_SERVER_URL` 仍最高优先级。5713 被占时释放端口或 `AGX_DEV_PORT=5715 npm run dev`；**打包 DMG/exe 走 `file://` 不受端口影响**。
- 分身由 `agenticx/avatar/registry.py` 与 `group_chat.py` 管理；群聊默认注入 Meta-Agent；`group_router.py` 负责 `user-directed` / `meta-routed` 路由与 mention 归一化，支持成员间 `@` 触发后续接话。文件类工具（如 `file_read`、`liteparse`）解析路径须与会话/用户配置的工作区根一致，不应仅在分身默认目录 `~/.agenticx/avatars/<avatar_id>/workspace` 下查找用户已加入工作区或通过 `@` 引用的仓库文件；`delegate_to_avatar` 委派时应继承主会话 `taskspaces`/`context_files`，否则相对路径易落到分身默认 workspace。
- `agenticx/llms/minimax_provider.py` 用 `@model_validator(mode="after")` 自动补 `openai/` 前缀；MiniMax M2 系列按非视觉模型处理，前端需拦截不支持的图片输入，Studio 需剥离 `image_inputs`。智谱 `glm-5` / `glm-5-*`（名称不含 vl、vision、4v、5v）按纯文本模型处理，Studio 同样剥离 `image_inputs`，Desktop 在 `model-vision.ts` 拦截附图。类 OpenAI 兼容代理（如 New API、LiteLLM 网关）若上游不支持 `tool_choice` 会触发 `UnsupportedParamsError`，需在代理侧开启 `drop_params`/兼容模式；API Base 填写习惯宜与 Cherry Studio 对齐（常见为根地址或带 `/v1`），设置 UI 宜提示避免路径歧义。
- 扩展生态位于 `agenticx/extensions/`：`bundle.py` 定义 AGX Bundle（skills/mcp_servers/avatars/memory_templates），`installer.py` 负责本地安装卸载，`registry_hub.py` 做多源聚合搜索；Studio 暴露 `/api/skills/*`、`/api/bundles/*`、`/api/registry/*`。Desktop 在 Windows 平台默认禁用 GPU 合成（`disable-gpu` + `disableHardwareAcceleration`）以规避重影与拖影；非 Windows 仍可用 `AGX_DISABLE_GPU=1` 强制禁用；Windows 安装时 `import resource` 模块不兼容，需条件导入。Computer Use 能力内化于 `agenticx/tools/fallback_chain.py`（`ToolFallbackChain`）、`agenticx/tools/resolvers/`（`MCPConnectorResolver`、`ComputerUseResolver`）、`agenticx/tools/policy.py`（`CategoryPolicy`）、`agenticx/embodiment/tools/desktop_adapter.py`（`DesktopPlatformAdapter`）、`agenticx/runtime/task_scheduler.py`；已有 plan `.cursor/plans/2026-03-24-computer-use-internalization.plan.md`。通过 MCP 接入的 browser-use 类浏览器工具在实战中出现已连接仍 `mcp_call` 失败、`ClosedResourceError`、参数校验错误或与不兼容的浏览器上下文 API 并存的情况，智能体可能回退到 bash/Playwright 等直连路径，排障时需对照连接状态、tool schema 与实际调用链。Skill 采用双路径注入：路径 1 为 Tool Result + 三层渐进披露，路径 2 为 System Prompt 注入（`active_skill`）；框架层推荐路径 1。模型回复中列举的 `mdfind`、`pdftotext` 等常为本机/环境 CLI 能力说明，与某 skill「已安装即会在当轮被调用」不等价，排障时对照运行时工具注册、会话可用工具集与 skill 激活状态。Studio/Machi 对话工具表含 `liteparse`（`agenticx/cli/agent_tools.py` 的 `STUDIO_TOOLS` + `dispatch_tool_async`），可与环境权限中的 `liteparse` key 对齐过滤；`LiteParseAdapter` 解析 CLI JSON 须优先读顶层 `text`，缺失则拼接 `pages[*].text`（旧版 CLI 只返回 `{"pages":[{"text":...}]}`，无顶层 `text`）；`liteparse` 工具须严格只走 `LiteParseAdapter`，不再回退到通用文本直读，否则 PDF 会被当乱码塞给模型触发 context window 超限；LiteParseAdapter 未安装时返回明确错误含安装命令（`npm i -g @llamaindex/liteparse`）。模型在 bash 中自行调用的 `mdfind`/`pdftotext` 是本机 CLI，与 Machi 内置 `liteparse` 工具无关。官网 `https://www.agxbuilder.com/`。
- hermes-agent codegen 已落地两个机制：(1) 跨会话 FTS5 检索（`agenticx/memory/session_store.py` 新增 `session_messages` + `session_messages_fts` 外部内容表 + 触发器；`index_session_messages` / `search_session_messages` / `backfill_from_sessions_root` 方法；`AGX_SESSION_FTS` 开关默认 `1`；`SessionManager.__init__` 在 event loop 运行时自动 fire-and-forget `backfill_from_sessions_root`，将历史 `~/.agenticx/sessions/<id>/messages.json` 增量写入 FTS 表；`session_search` 工具注册到 `STUDIO_TOOLS`，空 query 返回最近会话，有 query 则 FTS 聚合按 session 分组，limit 1–5）；会话摘要/元数据默认 SQLite 文件为 `~/.agenticx/memory/sessions.sqlite`（`DEFAULT_SESSION_DB_PATH`），排查会话标题等问题时勿与 `~/.agenticx/workspace/session_store.db` 等易混淆空库混用；(2) 技能生命周期管理（`agenticx/skills/guard.py` 新建，提供 `scan_skill` / `should_allow`，regex 扫描 exfiltration/credential/injection/destructive 四类危险模式；`skill_manage` 工具注册到 `STUDIO_TOOLS`，支持 create/patch/delete，`AGX_SKILL_MANAGE` 默认 `0` 需显式开启，agent-created 技能直接写到 `~/.agenticx/skills/<name>/SKILL.md`（不再加 `agent-created/` 中间层，`_agent_created_skill_root()` 已改为直接返回 `~/.agenticx/skills`）；`skill_manage` 的 `name` 字段支持子路径（如 `ima/notes`），`_safe_skill_dir_name()` 按 `/` 拆分逐段校验；`guard.py` 中 `exfiltration_curl` 正则已收窄为 `curl\s+.*\$\{?\w` 避免误拦普通 API 文档示例；create/patch 失败自动回滚）；对应冒烟测试：`tests/test_smoke_hermes_agent_*.py`（18 条）。
- Desktop `ChatPane` 输入区已从 `textarea` 迁移到 `contenteditable`；`@file` 选中后注入不可编辑行内 token（蓝色圆角标记），不再是纯文本占位符；`AttachedFile`/`MessageAttachment` 新增 `sourcePath` 和 `referenceToken` 字段（`desktop/src/store.ts`）；发送/重试时 `context_files` 优先使用 `sourcePath` 绝对路径；输入文本中删除 `@file` token 后对应引用不会进入 `context_files`；从工作区面板点选「引用文件」也触发行内 token 注入。`_run_delegation_in_avatar_session`（`agenticx/runtime/meta_tools.py`）已落地 `taskspaces`/`context_files` 从主会话同步到分身 session，且分身的 `taskspaces` 回写到 `managed.taskspaces` 避免下次请求覆盖旧值。
- Desktop 主进程外网请求与代理：Near 设置页里「检测密钥/拉取模型/健康检查」三个 IPC handler（`validate-key`/`fetch-models`/`health-check-model`）跑在 Electron 主进程，调用 `globalThis.fetch` 打境外 provider（mistral / openai / anthropic 等）。两个易踩的坑须长期记忆：(1) **Electron 主进程的 `globalThis.fetch` 不是 Node undici 的 fetch，而是 Electron 自带的 `net.fetch`（走 Chromium 网络栈），传 `dispatcher: ProxyAgent` 会被静默忽略**，结果即使解析到代理 URL 也照样直连；要让 `HTTPS_PROXY`/`HTTP_PROXY`/`ALL_PROXY` 在主进程生效，必须**显式 `import { fetch as undiciFetch } from "undici"`** 用它发请求，封装在 `desktop/electron/proxy-fetch.ts` 的 `proxyAwareFetch`，三个 handler 都改用它。(2) **`undici@7+` 不能在 Electron 34（内置 Node 20）里用**——启动期 `require('undici')` 即抛 `TypeError: webidl.util.markAsUncloneable is not a function`（7.x 用了 Node 22+ 才暴露的内部 webidl API），必须把 `desktop/package.json` 的 undici 钉到 `^6.x`（LTS 线，ProxyAgent API 与 7.x 一致）。Node 自带 fetch 不读 `HTTPS_PROXY` 是 undici 的官方行为，不是 Near 的 bug；以前用户唯一能让主进程出墙的办法是开 macOS 系统代理（Chromium 才走），这条改动后 export 变量也能用，与 `agx serve`（Python httpx/litellm）行为对齐。`proxyAwareFetch` 大小写 env 都试（大写优先），支持 `NO_PROXY` 后缀匹配（`example.com` 命中 `sub.example.com`，`*` 全部直连），`socks://`/`socks5://` 等 `ProxyAgent` 不支持的方案自动回退 `globalThis.fetch`；`app.whenReady()` 早期会调用 `logProxyConfig()` 打印一行 `[proxy] main-process fetch using ...` 便于排障，不打印密钥/Token。修 Electron 主进程网络相关代码后必须**完全 Ctrl+C 重启 `npm run dev`**——`tsc --watch` 会重编译 `dist-electron/`，但主进程不会热重载，仅刷新渲染进程看不到效果。对应 plan `.cursor/plans/2026-05-26-electron-fetch-proxy-support.plan.md`。
- browser-use MCP：`browser_extract_content` 在 MCP 子进程内需可用的 `OPENAI_API_KEY`（及与 browser-use 兼容的 `OPENAI_BASE_URL`/`OPENAI_MODEL`，纯文本模型即可）；会话删除或过期清理应关闭该 session 的 `mcp_hub`，否则易残留多个 `browser-use` stdio 进程争抢 CDP，表现为 `browser_get_state`/`browser_get_html` 失败或会话列表异常。browser-use 已从 auto-connect 中彻底排除（所有会话均不自动连接），需要时须显式调用 `mcp_connect`；用 `--profile Default` 可复用 Chrome 登录 Cookie，但操作前须先完全关闭 Chrome，否则 CDP 报"浏览器已被占用"。
- Docker 驱动的 MCP（如 GitHub MCP：`docker run` `ghcr.io/github/github-mcp-server`）依赖本机 Docker CLI 对 daemon 的响应；`docker info`/`docker run`/镜像拉取若长时间不返回，`mcp_connect_async` 在握手与 `discover_tools` 阶段可能一直挂起，会话侧 `op_message` 会停留在「连接中：握手并发现工具…」。Docker Desktop 托盘看似正常但 CLI 不秒回、或容器内 `HTTP(S)_PROXY` 指向不可用端口时常见此现象；排障时优先验证 CLI、镜像与代理配置。产品侧若缺乏整体超时或前端未在失败路径刷新 MCP 状态，失败也可能迟迟不展示。
- Desktop 当前硬绑本地后端（`spawnAgx(["serve", "--host", "127.0.0.1", ...])`），不支持连接远程 `agx serve`；`agx serve` CLI 支持 `--host`/`--port`/`--reload`，CORS 白名单硬编码 `localhost:5173` 系列；已有 plan `.cursor/plans/2026-03-24-desktop-remote-backend.plan.md` 规划远程模式支持。IM 远程指令（手机飞书/企业微信 → 云端 Gateway → 本机 Agent）见 `.cursor/plans/2026-03-30-im-remote-command-gateway.plan.md`：飞书机器人优先、微信侧走**企业微信自建应用**（明确不做个人微信 Hook）；Gateway 与 `agenticx/gateway/`（含飞书 Adapter 等）及 `~/.agenticx/config.yaml` 的 `gateway` 节、云端示例 `docs/gateway/gateway_config.example.yaml` 对齐该 plan。本机跑飞书长连接（`agx feishu`）时，`studio`/Studio URL 须指向 Machi 已启动时实际监听的 `agx serve` 地址（含动态端口如 `127.0.0.1:65133`），一般不必再单独开终端起一份 `agx serve`。飞书侧展示助手回复时，宜将 Markdown 解析为飞书 **post 富文本** 再下发，避免裸 Markdown 在飞书内不可读；飞书用户与 Machi 会话/分身的动态绑定持久化在 `~/.agenticx/feishu_binding.json`，由 `agenticx/gateway/feishu_longconn.py` 与 Desktop IPC 共读共写。`agx serve` 启动时将实际端口和 `AGX_DESKTOP_TOKEN` 分别写入 `~/.agenticx/serve.port` 和 `~/.agenticx/serve.token`，飞书 bot 等外部组件据此自动发现后端地址与认证令牌。`feishu_longconn.py` 中 `httpx` 对 localhost 的请求须用 `httpx.AsyncHTTPTransport()` 绕过系统 SOCKS/HTTP 代理，否则会触发 `RemoteProtocolError`。Desktop 端飞书自动启动由 `desktop/electron/main.ts` 的 `startFeishuProcess()`/`stopFeishuProcess()` 管理子进程生命周期，IPC 包括 `load-feishu-config`/`save-feishu-config`/`load-feishu-binding`/`save-feishu-desktop-binding`（`preload.ts` + `global.d.ts` 类型声明）。`App.tsx` 每 2s 轮询 `feishu_binding.json` 的 `_desktop` key，变化时自动切换或创建对应 pane（`/bind` → 弹出分身窗格、`/unbind` → 回切 Machi）；`ChatPane.tsx` 每 3s 轮询判断当前 session 是否需要外部刷新（`feishuDesktopBound` 或 `isImSession`）。飞书 badge 显示逻辑改为全局唯一：显式绑定 session 优先显示标记，无绑定时仅元 Agent pane 显示，`im-` 前缀会话不再自动打标；飞书绑定的 session 在历史面板中应置顶/独立区域展示。绑定关系应在用户浏览或切换历史会话等桌面操作时保持稳定，避免无意 UI 操作导致绑定 session 漂移；用户重新绑定或改绑后，网关/后端构造上下文须以当前绑定 session 的 `messages.json`（或等价持久化历史）为唯一来源，不得混入此前已解绑会话的消息。
- 分身颜色系统统一封装在 `desktop/src/utils/avatar-color.ts`：分身按头像颜色 hash 从 PALETTE 取色，群聊用独立 `GROUP_PALETTE`（indigo/teal/pink/lime/red/blue/yellow/purple）按列表 index 顺序分配颜色（不用 hash，防止撞色）；群聊头像为圆角正方形 `rounded-[6px]`，分身头像为圆形；pane 背景 tint 通过 `avatarTintBg()` 对分身和群聊统一处理；工具调用在 `ToolCallCard` 中渲染，默认折叠，仅 `inlineConfirm` 时强制展开。
- 技能来源标注与扫描路径可配置化已落地：`SkillMetadata` 新增 `source` 字段，`infer_skill_source()` 按 base_dir 派生来源（builtin/cursor/claude/agents/registry/bundle/agenticx/agent_created/custom）；`build_skill_search_paths()` 动态合并核心路径（始终扫描）+ 用户在 `~/.agenticx/config.yaml` 的 `skills.preset_paths`（预设开关）与 `skills.custom_paths`（自定义）；`GET/PUT /api/skills/settings`（含 `disabled_skills` ↔ `skills.disabled`）+ Desktop IPC `get-skill-settings`/`put-skill-settings`；`AvatarConfig.skills_enabled` 与 `POST/PUT /api/avatars` 持久化分身级覆盖；`get_all_skill_summaries`/`studio_skill.skill_use` 按全局禁用 + 分身映射双层过滤。前端来源 badge 颜色区分、内置技能默认折叠；`~/.agenticx/skills/` 整体作为核心扫描根（二级遍历：一级无 `SKILL.md` 则向下再扫一层）；`~/.agents/skills` 默认关闭（Anthropic Claude Code 生态路径，对 Machi 原生用户无必要）；`/plugins` 目录不由 Skills Tab 扫描（后续规划独立 Plugin tab）；对应 plan `skill-source-config-ux_84b3504d.plan.md`。
- ClawHub 注册表 API 域名须用 `clawhub.ai/api`（`clawhub.io` SSL 证书过期、`registry.agxbuilder.com` DNS 无法解析）；`RegistryHub.search()` 对单源请求异常仅打 `logger.warning` 后 `continue`，不向前端暴露错误——市场搜索结果为空时需对照 `agx serve` 日志排查具体源的失败原因。ClawHub skill 安装链路：`/v1/skills/{slug}` 只返回元数据（无 markdown 内容），下载须走 `/v1/packages/.../versions` → `/v1/versions/{ver}` → `/v1/download` 三步；`registry_install_preview` 会缓存 SKILL.md，后续 `registry_install` 优先复用缓存避免二次拉取；安装成功后技能列表应立即置顶刷新。
- GitHub Actions 构建 Machi 桌面（`.github/workflows/build-desktop.yml`）：`jobs.<job>.if` 中不可引用 `matrix`（需拆 job）；在单台 macOS runner 上无法可靠对 `agx-server` 做 arm64+x64 的 `lipo` universal；electron-builder 25.x 要求 `desktop/package.json` 含 `author`；CI 中 `electron-builder` 宜加 `--publish never`；无 Apple 签名证书时不向 env 传空的 `APPLE_*`/`CSC_*`，并设 `CSC_IDENTITY_AUTO_DISCOVERY=false`，避免在未签名构建里启用 `hardenedRuntime`/entitlements 或误配 `afterSign` 触发签名校验；推送含 workflow 变更需 PAT 含 `workflow` scope。Runner 使用 Python 3.12 时标准库已移除 `distutils`，在 `setup-python` 之后应 `pip install setuptools`，否则 `node-gyp` 编译 `node-pty` 可能失败。macOS CI 仅构建并 upload-artifact 上传 `.dmg`（`electron-builder.yml` 已去掉 zip target，不做自动更新用 zip）。矩阵中 arm64 常用 `macos-14`，Intel/x64 须用 `macos-15-intel`（`macos-13` 已退役不可用）。`workflow`/`job` 的展示名称宜反映实际用途或矩阵维度（如架构），避免长期固定为单一泛化文案。Node20-based actions（`actions/checkout`、`actions/setup-node`、`actions/setup-python`、`actions/setup-go`、`actions/upload-artifact` 等）需定期升级至 `@v5` 以消除 Node 20 弃用 warning 并避免 `actions/checkout@v4` 在 GitHub hosted runner 上 `Post Run` 阶段的 git fetch 失败。
- 对话中触发的 `/codegen <upstream_name>` 工作流：研究物料归 `research/codedeepresearch/<upstream_name>/`，按对应 proposal 的 P0→P1 顺序推进，每一功能点须有可运行的 `tests/` 冒烟测试；新增依赖尽量克制，集成上游代码须在文件头或 `NOTICE` 等处标注来源与许可证。
- 个人微信（iLink）接入采用 **Go sidecar + HTTP API 桥接**架构：`packaging/wechat-sidecar/` 为独立 Go module，依赖 `openilink-sdk-go`，编译为单独二进制由 Machi Desktop 管理子进程（类飞书 `startFeishuProcess` 模式）；sidecar 暴露 `/bind/start`（QR 绑定）、`/bind/{id}/ws`（WebSocket 轮询扫码状态）、`/events`（SSE 消息推送）、`/send`（发送回复）等 HTTP 端点；凭据持久化到 `~/.agenticx/wechat_credentials.json`，端口写 `~/.agenticx/wechat_sidecar.port`；后端适配器 `agenticx/gateway/adapters/wechat_ilink.py`（`WeChatILinkAdapter`）连接 sidecar SSE 并中继消息到 agent runtime；`config.yaml` 配置节 `gateway.adapters.wechat_ilink`；Desktop IPC 含 `start-wechat-sidecar`/`stop-wechat-sidecar`/`wechat-bind-start`/`wechat-bind-status`；iLink `context_token` 24h 过期需续期机制；对应 plan `.cursor/plans/2026-04-03-wechat-ilink-integration.plan.md`、研究产物 `research/codedeepresearch/openilink-hub/`。sidecar 预编译二进制不纳入 Git：`packaging/wechat-sidecar` README 须写明首次开发执行 `make build`，产物路径由 `.gitignore` 忽略；若曾误跟踪用 `git rm --cached` 后单独提交。本机用 `curl`/HTTP 探测 `127.0.0.1` 与 `wechat_sidecar.port` 时，若 shell 配置了 `all_proxy`/`http_proxy` 等，须对 localhost 使用 `--noproxy '*'` 或等价方式，避免经 SOCKS 转发出现空响应（与飞书侧 httpx 绕代理问题同类）。
- `/update-conclusion` 自定义命令位于 `~/.cursor/commands/update-conclusion.md`：以「核心代码路径」下的 git 变更为输入（模式 A 基于最新 commit，模式 B 通过 `--plan=<plan_file_path>` 聚合同 Plan-Id 的多 commits，与 `/commit --spec` 联动），自动维护「代码 conclusion 路径」下各模块总结文件的新增/修改/删除，回复中须按模块聚合简要列出识别到的 git 变更与结论文件关键增删改，不堆泛化描述。
- `/genreleasenote <旧版本> <新版本>` 自定义命令（`.cursor/commands/genreleasenote.md`）：基于 `pyproject.toml` 的版本 bump commit 划定区间，按 scope 分类归纳提交后生成英文 GitHub Release Notes；`docs/release-note-guide.md` 规范了 Release Note 撰写标准（用户视角、一句话价值、诚实分级、合并同类项）。Release Notes 顶层必须拆分为两节：`## Highlights` 只放 `feat` 的新能力/新集成（同一 feat 的后续 `fix` 合并进 Highlights 不重复列），`## Fixes & Improvements` 放独立 `fix` / `perf` / 可见 `style` 条目；两节内部按 `Core / Desktop / IM / Skills / KB / Automation / MCP / Studio / Packaging/CI` 顺序分模块；若某节没有条目可整节省略，避免把 feat 与 bugfix 混入同一 highlights 导致读者分不清版本定位。
- 声明式 Hooks：`GET /api/hooks` 返回 `curated_hooks`（`agenticx/hooks/bundled/` 下各包的 `HOOK.yaml`）、去重后的 `imported_hooks`（`agenticx/hooks/loader.py` 中 `deduplicate_hooks` / `classify_hook`，含 `duplicate_count`、`usability`）与 `scan_summary`；Desktop `SettingsPanel` 的 Hooks Tab 以「预置钩子」为主区、外部扫描默认收在折叠的「外部导入」内。Hooks 设置页在视觉上应统一为单一 Tab 内的信息架构，避免预置与来自 Cursor/Claude 等路径的导入钩子呈现为割裂区块；按事件（如 `sessionStart`）展开时，来自外部工具路径的钩子应归入**同一事件容器内**展示，而非与事件块平级的重复分区。列表与分组样式宜避免粗白边框与过重「来源分组」条；对多来源、同名钩子宜通过治理与去重在产品侧内化汇总，展示语义对齐内置工具（减少同屏多来源堆叠带来的噪声与不可预期感）。系列 hooks 相关开发前宜与用户对齐分阶段任务与验收口径（先规划再动手）。Machi/Studio 对话运行时经 `agenticx/runtime/hooks/legacy_event_bridge_hook.py` 等与 `agent_runtime` 接线，派发 `agent:start`/`stop`、`tool:before_call`/`after_call`，bundled 的 `pre_tool_guard` 等依赖 `tool:before_call` 才能拦危险 shell；`rm -rf` 类匹配需覆盖合并 flag 写法并避免在整段命令（如带引号的 commit message）上误伤，排障时对照钩子注册与正则语义。
- Desktop 定时任务「触发执行」会话的 `avatar_id` 形如 `automation:<task_id>`：`agenticx/studio/server.py` 为执行器注入的系统提示须明确「执行阶段、非建任务阶段」，禁止再询问调度频率/时间；该会话可用 Meta 工具须过滤掉 `schedule_task` / `list_scheduled_tasks` / `cancel_scheduled_task` 与 `delegate_to_avatar`，避免触发态误判文案后递归建任务。
- 定时任务单次运行文本日志：`~/.agenticx/logs/automation/<task_id>.log`，Electron 主进程经 IPC 可读尾部；Meta 只读工具 `get_automation_task_logs` 定义于 `agenticx/runtime/meta_tools.py`。
- `SessionHistoryPanel`：`listSessions` 在 Meta 窗格（`avatarId` 为空）等场景可能返回全量会话，须在拉取后按 `avatar_id` 二次过滤——Meta 仅展示 `avatar_id` 为空或未设置的会话；`automation:*`、群聊与分身窗格仅展示与当前 pane `avatarId` 一致的会话，避免自动化会话混入元智能体历史列表造成「串会话」观感。会话列表渲染与切换时须用已持久化的标题字段覆盖占位 id，避免短暂出现中文标题后因缓存或快照不含标题而回退为数字 id。
- 技能自进化（Learning）子系统位于 `agenticx/learning/`：`observer.py` 在运行时捕获每次 tool call 观察并写入 `~/.agenticx/sessions/<session_id>/tool_call_observations.json`（旧路径 `~/.agenticx/instincts/projects/<project_id>/observations.jsonl` 已废弃可删）；`session_review_hook.py` 在 `on_agent_end` 后台从同一 session 目录直接加载观察（不再按 `project_id` hash 过滤）；`config.py` 集中读取 `config.yaml` 的 `learning:` 节（含 `enabled`/`nudge_interval`/`min_tool_calls`/`auto_create`/`quality_gate_min_score`/`review_model` 等），`AGX_LEARNING_ENABLED` 环境变量同时控制观察记录与会话复盘（原 `AGX_SKILL_REVIEW_ENABLED` 与 `skill_review_enabled` config key 已移除）；Desktop 设置面板将「启用观察式学习」与「启用技能复盘审查」合并为单个「启用技能自进化」开关（`learning_enabled`），卡片内联显示观察数据存储路径。`skill_quality_gate.py`（5 项检查：min_steps / success_evidence / dedup / guard_scan / actionability）、`skill_usage_tracker.py`（per-skill `.usage_stats.json`）、`skill_deprecation.py`（低效技能标记）、`skill_condition_filter.py`（按 `requires_tools`/`requires_toolsets`/`fallback_for` frontmatter 过滤会话可见技能）协同构成技能生命周期管理闭环。`agenticx/skills/fuzzy_patch.py` 提供 5 策略模糊匹配链（exact → line_trimmed → whitespace_normalized → indentation_flexible → escape_normalized）用于 skill patch；`agenticx/skills/versioning.py` 在 `<skill_dir>/.changelog` 记录每次 create/patch/delete 变更日志。`observer.py` 写入观察时的 `success` 须据工具返回内容推断，不可长期硬编码为 True，否则失败/重试类学习信号与门禁不可用。
- Machi Windows 桌面打包与 mac DMG 流程并行：入口脚本 `packaging/build_windows_installer.ps1`（由 `.github/workflows/build-desktop.yml` 中 Windows 矩阵调用），产物为 NSIS 安装包；期望与 DMG 同级——**内嵌 `agx-server` 后端 + 内嵌 Python 运行时与关键向量库依赖（`chromadb`、`onnxruntime` 等）**，终端用户安装后不应再触发 `pip install chromadb` / `pip install onnxruntime` 之类的运行时依赖，PyInstaller spec 须显式 `collect_all` 或 hiddenimports 这些依赖；ChromaDB 在 `upsert` 时 metadata 字段不接受 `None`（会抛 `Expected metadata value to be a str, int, float or bool, got None`），入库前须过滤/回落默认值；打包后首次加载持久化目录配置迁移元数据时还可能抛 `KeyError: '_type'`（`chromadb/db/impl/sqlite/sysdb.py:_load_config_from_json_str_and_migrate` → `configuration.py:from_json_str`），该问题在 `npm run dev` 不复现，PyInstaller spec 须 `collect_all('chromadb')` 且完整打入其迁移脚本/资源目录，并在读取异常时回退/重建而非直接向用户透出单 token `'_type'`。`bash_exec` 在 Windows 上直接调 `/bin/bash` 会触发 `WinError 2`（系统找不到指定文件），需解析可用 shell（`where bash`/`pwsh`/`cmd`）并做平台分支，Issue #7 即此类问题。Desktop 设置页对 `provider.displayName` 等来自 provider 清单的字段须做 undefined 防御（可选链 + fallback），否则 Windows 打包后首次进入设置页会崩溃 `Cannot read properties of undefined (reading 'displayName')`（这类缺陷在 DMG 构建上不一定复现，必须单独在 Windows exe 上回归）。Machi 桌面图标须通过规范化脚本（`packaging/` 下）统一导出 `.icns`/`.ico`/多尺寸 PNG，避免 `npm run dev` 与 DMG/NSIS 安装后图标尺寸视觉不一致。DMG 安装包在他人机器启动 MCP 时出现 `[Errno 2] No such file or directory`（GitHub issue #14）多为打包环境 PATH 不全或外部二进制未内嵌所致：MCP stdio 子进程（如 `npx`、`node`、`docker`、`uvx` 等）的启动逻辑须健壮解析可执行路径（`shutil.which` + 常见安装位置 fallback），检测不到时给出明确「未找到 X 命令，请安装或配置 PATH」而非裸 `Errno 2`；自带 sidecar 类依赖应在 PyInstaller spec / electron-builder `extraResources` 中显式打包。
- 本机 Claude Code（CC）桥接：`agenticx/cc_bridge/` 提供本地 HTTP 服务（NDJSON 流），CLI 入口为 `agx cc-bridge serve`；Studio/Machi 在 `agenticx/cli/agent_tools.py` 的 `STUDIO_TOOLS` 中暴露 `cc_bridge_start` / `cc_bridge_send` / `cc_bridge_list` / `cc_bridge_stop` / `cc_bridge_permission`，经 `AGX_CC_BRIDGE_TOKEN` 或 `~/.agenticx/config.yaml` 的 `cc_bridge.token` 及 `cc_bridge` Base URL（见 `agenticx/cc_bridge/settings.py`）与 bridge 进程通信；协议说明见 `docs/cc-bridge-protocol.md`，计划见 `.cursor/plans/2026-04-08-cc-local-bridge-machi.plan.md`。Desktop 侧 `cc_bridge_start`/`cc_bridge_send` 的 UI 须以**当次启动解析出的 mode** 为准：`headless` 不得仅因设置页「Visible TUI」默认偏好而自动拉起 cc-bridge/claude-code 侧栏终端；`visible_tui` 才应 attach PTY；`cc_bridge_send` 的进行中提示须随 mode 区分（无头时说明无需在侧栏终端按键，可见模式再提示焦点进终端），相关文案与解析宜集中在 `desktop/src/utils/cc-bridge-ui.ts` 并与 `ChatPane`/`ChatView` 一致。
- 面向客户交付时，用户倾向采用「通用产品内核 + 客户定制隔离」架构：通用能力沉淀在可复用主产品中，客户专属实现放在隔离目录/私有仓或可剥离层，不直接混入开源主仓主线代码。
- MCP 市场“已添加”状态判定不能仅比较 `item.id` 与 `mcpServer.name`：两者常不一致（如包名 vs 实际 server key）。前端需结合安装返回的 `installed/updated`、`mcpMarketplaceDetail` 里的 `server_config[].mcpServers` key 建立映射，并在 `SettingsPanel` 通过 `agenticx:mcp:marketplaceIdToNames` 本地持久化后再匹配。
- `enterprise/scripts/start-dev.sh` 现已作为 enterprise 本地开发默认入口（默认仅拉起 `web-portal:3000` 与 `admin-console:3001`，`--all` 才包含 `customers/*`，支持 `--ui=stream`）；若手动执行 `pnpm exec turbo run dev`，除 `source .env.local` 外还需显式导出 `AUTH_JWT_PRIVATE_KEY` / `AUTH_JWT_PUBLIC_KEY`（由 `*_FILE` 读取 PEM 内容），否则前台登录会报缺少 JWT key。admin-console 登录走 `ADMIN_CONSOLE_LOGIN_PASSWORD`（默认 `change-me`）；web-portal 开发态 owner 登录走 `AUTH_DEV_OWNER_PASSWORD`，首次启动后默认管理员账号 `admin@agenticx.local`。管理台登录页「企业 SSO 登录」按钮在无对外展示的 SSO 提供方时为禁用态：`ssoProviders` 来自 `getAdminSsoProviderOptions()`（读 `NEXT_PUBLIC_SSO_PROVIDERS`，格式 `id:显示名`、多个逗号分隔）；本地需在 `enterprise/.env.local` 等处配置后**重启 admin-console** 方可见效。配套脚本 `enterprise/scripts/start-dev-with-infra.sh` 是 `start-dev.sh` 的**超集**：先以 docker-compose 拉起 Postgres/Redis 等中间件再起应用栈，`--skip-infra` 仅启应用栈（早期版本曾出现 `APP_ARGS[@]: unbound variable` 的 `set -u` bug，须保留经过初始化）；陡然新建对话/历史拉取报 `chat history operation failed` 多为未先起中间件导致 PG 不可达。
- Enterprise 2026-05 生产化波次：身份权限/聊天历史 + Token 用量/网关审计/策略规则中心已从 mock 切换为 PG 真落库，对应 plan：`.cursor/plans/2026-05-04-enterprise-iam-full-buildout.plan.md`、`2026-05-03-enterprise-chat-history-persistence.plan.md`、`2026-05-04-gateway-audit-production.plan.md`、`policy-rule-center_181c93c7.plan.md`。IAM 批量导入 CSV 须含部门字段（`dept_id` 等）；前台/后台展示的 token 用量与历史会话必须以 PG 单一来源为准（曾出现「后台 N 条使用记录、前台 0 条历史」对不上号、「调了模型仍显示 0 token」的 bug，需确认模型调用真实回写 usage）；网关审计 admin-console UI 走 PG 主写 (`gateway_audit_events`) + JSONL 兜底 + 真 RBAC（`audit:read:all` / `audit:read:dept`），不再使用 mock store。
- Enterprise 运行时配置（模型服务 / 用户可见模型 / Token 配额）PG 化后单一来源为 `enterprise_runtime_*` 表；legacy JSON（`.runtime/admin/*.json`）仅作一次性导入源，共享迁移逻辑在 `@agenticx/iam-core/runtime-legacy-migrate`，CLI 为 `pnpm -C enterprise migrate:legacy-runtime`，`bootstrap.sh` 与本地 `start-dev.sh` 在 `db:migrate` 后自动执行；新增 runtime 配置表时必须 admin/portal/CLI 三端共用同一 migrator，避免「只有某一端 lazy import」导致前台空模型。
- 在 enterprise 视图里发现「保存了拦截却显示 blocked=false」「策略规则点删除直接消失」「左侧导航不跟随路由」「token 永远 0」类 bug 时，多与「前后端动作语义未对齐 / PG 中间件未起 / 未发布规则被硬删 / 模型 usage 未回写」有关；先按 `start-dev-with-infra.sh` 拉起中间件、确认 `policy-snapshot.json` 路径与 gateway 实际读取一致、并核对 admin-console store 是否仍为旧版内存态，再做代码层修复，避免漏掉环境根因。
- Enterprise UI 设计 token 单一来源统一到 `enterprise/packages/ui/src/themes/base.css`（Tailwind v4 `@theme inline` + `@custom-variant` + `tw-animate-css`，OKLCH `indigo/violet` primary），旧 `machi.css` / `default.css` 已删除，`--machi-*` 保留 alias 作渐进迁移。`useUiTheme` 已支持三态（`system/dark/light`）并暴露 `resolved` 与 `toggle()`，`<html>` 预注入脚本防 FOUC——修复了此前 toggling `<html class="dark">` 但样式表走 `.machi-light`/`.theme-dark` 导致主题切换**完全失效**的历史 bug。`@agenticx/ui` 新增 10 个原语 Popover / Checkbox / Progress / PageHeader / Breadcrumb / EmptyState / StatCard / DataTable（`@tanstack/react-table`）/ Toaster（`sonner`）/ Command（`cmdk`），17 个既有原语（Button/Badge/Card/Input/Textarea/Alert/DropdownMenu/Dialog/Sheet/Table/Select/Tabs/Tooltip/Skeleton/Separator/Avatar/Label）已迁到语义 token；新增依赖含 `lucide-react`、`cmdk`、`sonner`、`@tanstack/react-table`、`@radix-ui/react-popover`、`@radix-ui/react-checkbox`、`tw-animate-css`。AppShell v2 提供分组侧栏 + 活跃项左侧彩条、⌘K 全局命令面板、面包屑、三态主题下拉，折叠状态持久化到 localStorage。
- 若聊天输入区为无边框、由外层容器承托视觉（如 borderless `InputArea`），在 `base.css` 中勿把 `textarea` 放进**全局** `focus-visible`/`ring`（`var(--ring)`）选择器：该规则可能晚于工具类层叠，单在组件上 `outline-none` 仍可能出现意外蓝环；应收窄全局选择器或去掉其中的 `textarea`，由输入区/容器级样式承担焦点态。
- Enterprise UI 开发常见 Radix / React / recharts 陷阱：`@radix-ui/react-dialog` 要求 `DialogContent` 内必须含 `DialogTitle`，`CommandDialog` 须在内部挂一个 `sr-only` 的 `DialogHeader`/`Title`/`Description` 保 a11y；`DialogOverlay` 的 `backdrop-blur-sm` 在命令面板上会让整页发糊，`DialogContent` 暴露 `overlayClassName` 允许调用方切换成不带 blur 的轻遮罩；循环 `map` 返回 Fragment 时须用 `<React.Fragment key=...>` 而非匿名 `<>...</>` 以避免 `Each child in a list should have a unique "key" prop` 警告；recharts v3 的 `tick` 严格校验类型，须传 `{ fill, fontSize }` 对象而非 `CSSProperties`。
- Enterprise 视觉 e2e 验收脚本 `enterprise/scripts/e2e-visual-tour.ts`（`pnpm visual-tour` / `pnpm visual-tour:install`）依赖根 `tsx` devDep + 本机 Chromium + 本地 dev server（先 `bash scripts/start-dev.sh`），成功后在 `enterprise/docs/visuals/v2/{page}-{theme}.png` 输出 13 页 × dark/light 共 26 张全页长图，用作 PR 描述与视觉回归对比；脚本内置 probe（含 fetch 超时）检测 dev server 起没起，登录阶段需先导出 `ADMIN_CONSOLE_LOGIN_PASSWORD` 与 `AUTH_DEV_OWNER_PASSWORD` 否则会卡在 `page.waitForURL timeout`。v2 视觉重塑的 4 个参考 commit（分支 `feat/enterprise-visual-overhaul-v2`）为 `ccd46eb` ui-foundation → `ca6699e` admin-revamp → `5b47182` portal-revamp → `3b73e87` 验收。
- Enterprise Gateway 与 Edge-Agent 真实完成度（重要，用于客户方案回溯）：Go 网关 `gateway/internal/server/server.go` 已实现请求 / 响应 / 流式**三通道策略评估**（L133/L185/L279）与 Blake2b 哈希链审计、RS256 JWT 透传 tenant / dept / user / session 四主体。**Enterprise `apps/gateway` 上游模型调用在进程内为 Go 标准库 `net/http` 的 OpenAI 兼容客户端（如 `OpenAICompatibleProvider`），不嵌入 LiteLLM；与 Machi/Studio 对话侧 AgenticX Python 栈中以 `litellm` / `LiteLLMProvider` 统一多厂商路由是两条线，勿混为一谈。**但规则包 `extends` 在 Go `loader` 中类型为**单字符串**（Hechuang 自定义 manifest 用数组会反序列化失败或只取首项），Go 引擎只识别 `keyword` / `regex` / `pii`（不含 `keyword-list`），17 种密钥检测仍留在 AgenticX Python 框架**未进** Go 网关；`packaging/edge-agent` Go 单二进制当前仅约 33 行是**空壳 skeleton**，Desktop 实际链路为 Electron → 内嵌 `agx-server`（PyInstaller Python 后端，`127.0.0.1` 随机端口）→ Ollama，**不经**独立 Edge-Agent sidecar——宣传端侧闭环时描述应用「本地后端服务」等中性措辞，不可直接套用 Edge-Agent 概念。Chat/Gateway 侧审计链路：append-only JSONL 为必须成功兜底；配置了 `DATABASE_URL` 时对 PG 表 `gateway_audit_events` **best-effort** 双写，失败记 `.runtime/audit/.pg-pending`，启动回灌窗口由 `GATEWAY_AUDIT_BACKFILL_DAYS`（默认 7）控制；与 IAM CRUD 用的 `audit_events` 表**分表**；admin-console 查询/导出走 `PgAuditStore`与 `requireAdminSomeScope`，可见域依赖 `audit:read:all` / `audit:read:dept` 等 scopes（仅用旧 `audit:read` 的部门审计场景会403，需在库内升级角色 scopes）；全表链校验接口需 `audit:read:all`。历史缺口备忘：`AuditPolicyHit` 字段结构是 `matched_rule` + `severity` / `action`（不是 `rule_id` / `reason`）；若仍存在纯 `LocalAuditStore`/链断裂 UX 的旧路径，仍以实际部署代码为准排查。
- Enterprise 策略前后台联调（admin ↔ gateway）：控制台发布生成的 `policy-snapshot.json` 默认在 `enterprise/.runtime/admin/`；`enterprise/apps/gateway` 进程读到的是否为**同一路径**取决于默认配额根（如 `internal/quota/tracker.go` 的 `DefaultConfigPath` 所相对的 `../../.runtime/`），勿误指仓库根 `.runtime` 否则网关长期空快照、表现为「规则已保存/发布但前台仍不拦截」。仅**已发布（active）**规则进入快照；规则适用范围若填写 **userIds** 占位示例（如 `u1,u2`）会导致真实 JWT 用户不匹配而不生效，全员宜留空。策略 **block** 命中时网关多在请求阶段直接返回业务错误；若仍收到上游模型长篇「内容不当」类自然语言拒答，多与**模型自带安全**有关，需与网关策略命中区分。
- 仓库目前**没有**任何压测脚本或历史性能基线数据，客户投标承诺「≥200 并发 P95 ≤ 800ms」等指标须改为「提供压测方案 / 部署建议 / 历史项目参考」而非现场压测；本地知识库后端 `hybrid_search.py` 已支持 BM25 / vector / hybrid 三路检索，但 Desktop 调试面板默认只暴露单路检索，三模切换 UI 未开放；`documents` / `watermark` 等企业插件（文档校对、百页 PDF 水印保真）当前仍为模板 shell 未实现，不可作为现场演示项，MCP Marketplace UI 的 install/installedIds/installingId 状态管理已落地可演示。
- Enterprise 企业 SSO 走 OIDC：起点路由 `enterprise/apps/admin-console/src/app/api/auth/sso/oidc/start/route.ts` + `enterprise/apps/web-portal/src/app/api/auth/sso/oidc/start/route.ts`，运行时与 IdP discovery 在 `enterprise/apps/admin-console/src/lib/admin-sso-runtime.ts` / `enterprise/apps/web-portal/src/lib/sso-runtime.ts`，错误码常量集中于 `enterprise/packages/auth/src/services/oidc-error-codes.ts`；通过 `NEXT_PUBLIC_SSO_PROVIDERS`（`id:显示名` 列表）+ 各 provider 的 `issuer` / `client_id` / `client_secret` 等环境变量驱动，缺失会抛 `oidc.missing_env.default.issuer`，IdP discovery URL 不可达时抛 `Failed to discover OIDC metadata.`；新增或修改任何 SSO Provider / `NEXT_PUBLIC_SSO_PROVIDERS` 后**必须完整重启 admin-console / web-portal Next.js 进程**才生效，热更新不会捕获；面向运营商客户（中移动等）首选走运营商已有 IDaaS（OIDC client 模式），不必自建完整 OAuth2 server。开发态默认管理员账号已从 `owner@agenticx.local` 切换为 `admin@agenticx.local`，旧 owner 账号不会自动迁移密码 hash，须在「用户管理」用旧 hash 重建或在 PG 直接复制 `password_hash` / 走重置密码流程，否则旧密码无法登录新账号。
- AI 网关 (`enterprise/apps/gateway` + `gateway/internal/quota/tracker.go` 等) 当前配额管理以**租户级**为主，admin-console「额度控制」页仍偏**查询展示**；用户多次提出按 部门 / 用户（账号）维度设置 TPM / QPM / 并发等配额的诉求，真正限流落地需评估独立 plan，不可在客户对接 / 方案文档中口头承诺「已支持部门 / 用户级配额」。
